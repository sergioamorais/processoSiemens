Enunciado

9. Escreva uma função pesquisa_linear que tem como argumento de entrada: 
um vetor de inteiros a ser pesquisado, o tamanho do vetor, e uma chave (um valor inteiro) a ser procurado.  
A função retorna um inteiro: o índice do elemento do vetor (se a chave for achada) ou -1 caso contrário.
Além disso, mostre um exemplo de uso desta função em um programa com a chamada da função criada
--------------------------------------
Resposta (pseudo-codigo):

%Definicao da funcao, com os argumentos solicitados, sendo "vetor" uma string, "size" um inteiro correspondente às suas dimensões e "key" a chave a ser procurada 
def pesquisa_linear (vetor, size, key):

%Rotina que percorrerá o vetor para encontrar o indice que se refere a chave procurada
%Considerei a iteracao sobre um vetor, mas imagino que possa exitir alguma funcao "index" que captura o indice correspondente a um dado valor
k=0;

while (k<=size)
  if (vetor(k)==key)
    y=k;
  
  else
    k=k+1;
    y=-1;
    
  end
  
  k=k+1;
  
end

return y

%A funcao retorna o indice da chave a ser procurada ou "-1" caso essa nao seja encontrada.
